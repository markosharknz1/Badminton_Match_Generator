*.log
game_scheduler.db
exports/
logs/
.edge-app-profile/
__pycache__/
# PyInstaller output - build_exe.bat regenerates it for local use. The exe
# is NOT committed: releases are built by GitHub Actions from the tagged
# source (.github/workflows/release.yml) so the download is verifiably
# built from this code, not from a developer machine.
build/
dist/
*.spec
GameScheduler.exe
